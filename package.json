{
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "api": "json-server db.json --port 3001",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.20.0"
  }
}
